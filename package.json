{
  "name": "abdl-space",
  "version": "5.1.7",
  "private": true,
  "scripts": {
    "dev": "cd client && npx vite",
    "build": "cd client && npx vite build",
    "preview": "cd client && npx vite preview",
    "deploy": "cd client && npx vite build && npx wrangler pages deploy"
  }
}
