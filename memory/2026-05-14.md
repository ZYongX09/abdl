# ABDL Space 迭代日志 — 2026-05-14

## 迭代 1 (22:39)
- 修复自定义 .card 与 daisyUI .card 样式冲突
- 新增 cardSlideIn 入场动画 + stagger-item 交错动画
- 按钮交互增强：hover 上浮 + click 缩放 + 蓝色阴影
- 页面切换动画缓动优化
- 滚动条样式优化
- push 成功: 30c577d

## 迭代 2 (22:44)
- 修复纸尿裤页面搜索栏布局：join → flex flex-wrap gap-2
- 筛选标签改用 badge badge-ghost
- 清除按钮改用 btn btn-ghost
- push 成功: 9a7e8da

## 迭代 3 (23:01)
- 清理 CSS 重复定义和动画冲突
  - 移除死代码：.mobile-bottom-nav/.mobile-bottom-nav-inner（组件已用 daisyUI btm-nav）
  - 移除多彩主题中无效的 btm-inner 样式
  - 合并 3 处 .tag 定义为 1 处，统一使用 CSS 变量
  - 修复 .btn:active 动画冲突（tapFeedback 动画 vs transform:scale）
  - 移除重复的 buttonPop 关键帧
  - 修复 .stagger-item 入场动画被 cardFloatIn 覆盖的问题
  - 移除重复的 .diaper-card transition 定义
  - CSS 总量：112.90 KB → 110.19 KB（-2.7 KB）
- commit: 1fd53cd
- push 失败（网络不可用）

## 迭代 4 (23:07)
- 主题切换从二元(light/dark)升级为三元循环(light → dark → colorful)
  - 侧边栏新增主题切换按钮，显示当前主题名称(☀️浅色/🌙深色/🎨多彩)
  - 设置页和侧边栏主题切换状态通过 CustomEvent 同步
  - Ctrl+Shift+T 快捷键同步支持三主题循环
- 页面路由切换动画优化
  - 使用 key={location.pathname} 触发重新挂载，实现平滑 fade+slide 动画
  - 新增 pageSlideOut 退出动画，过渡更自然
  - 动画时长从 0.5s 优化至 0.4s，更流畅不拖沓
- About 快捷键说明更新
- commit: d00afe8
- push 成功: d00afe8

## 迭代 5 (23:16)
- 将自定义 .tag 和 .score-badge 类迁移至 daisyUI badge 组件
  - Home.jsx: .tag → badge badge-ghost badge-sm, .score-badge → badge badge-warning badge-sm
  - DiaperDetail.jsx: 所有 .tag 实例迁移至 daisyUI badge，管理员标签改用 badge-error
  - Profile.jsx: 管理员/注册日期标签 → badge-accent/badge-ghost
  - Recommendations.jsx: 身材数据标签 → badge-ghost
  - ComparePage.jsx: 已选纸尿裤标签 → badge-ghost
  - UserPage.jsx: 角色/日期标签 → badge-accent/badge-ghost
  - GuessYouLike.jsx: .score-badge → badge badge-warning badge-sm
  - 涉及 7 个文件，24 行改动
- push 成功: 49d390d
- 移除未使用的 .score-badge CSS 定义（-161 字节）
- push 成功: 7e18e77

## 迭代 7 (23:31)
- 修复全局 textarea/select 元素使用错误的 daisyUI 组件类名
  - DiaperDetail.jsx: 评价文本域 input→textarea，尺码选择 input→select
  - Profile.jsx: 简介文本域 input→textarea
  - AdminPage.jsx: 6处 wiki/术语文本域 + 1处产品类型选择框
  - 影响：这些元素之前用 input 的样式，缺少 textarea/select 的原生功能（如多行调整、下拉箭头）
- Rankings 页面添加 hero-card 头部
  - 与 ForumFeed、Login、Register 页面风格统一
  - 渐变背景 + 大图标 + 标题 + 副标题
- DiaperDetail 返回按钮改用 btn btn-ghost btn-sm，更精致
- push 成功: 8515099

## 迭代 6 (23:22)
- Login/Register 页面添加渐变 hero 头部区域
  - 宝宝蓝渐变背景 + 大图标 + 欢迎标语
  - 使用 auth-page-wrapper 装饰浮动圆圈动画
- ForumFeed 页面添加 hero 卡片头部
  - 社区图标 + 标题 + 副标题
  - 发帖表单添加 formSlideDown 入场动画
- CSS 新增动画：authFloat、authPulse、formSlideDown
- Hero card 添加半透明光斑 ::before 装饰
- 卡片 hover will-change 优化
- 涉及 4 个文件：Login.jsx、Register.jsx、ForumFeed.jsx、App.css
- commit: 196490b
- push 成功: 196490b

## 迭代 8 (00:07, 5月15日)
- **性能优化：缩小全局 transition 规则范围**
  - 旧规则：对页面上几乎所有 HTML 元素（div, span, p, h1-h6, li, section, article, nav, header, footer 等）应用 `transition: background-color/border-color/color 0.3s`
  - 新规则：仅对 ~25 个实际需要主题切换过渡的元素（.card, .btn, .input, .sidebar 等）应用
  - 效果：消除了数百个 DOM 元素上不必要的 transition 计算，页面渲染更流畅
- **清理死代码**
  - 移除空的 LiquidGlass CSS 规则块（仅含注释，无实际样式）
  - 移除重复的 `.stagger-item` 基础定义（被后面的 `cardFloatIn` 覆盖）
  - 移除重复的 `staggerFadeIn` 关键帧（与 `cardSlideIn` 完全相同）
- CSS: 112.01 KB → 111.92 KB (-90 bytes，主要收益在性能而非体积)
- commit: dd123d8
- push 成功: dd123d8

## 迭代 9 (00:16, 5月15日)
- **统一 hero-card 头部风格**
  - Home 页搜索区域从 `card bg-primary/10` 改为 `hero-card`，添加图标+标题+副标题布局
  - Settings 页新增 hero-card 头部（图标+标题+副标题）
  - Recommendations 页 hero-card 布局优化，图标+标题改为 flex 行排列
  - Login/Register 移除冗余的 inline `background: var(--hero-bg)` style（hero-card CSS 已有）
- **统一未登录状态**
  - Profile/Messages/Notifications 页未登录状态从普通 div 改为 hero-card 风格
  - 每个页面使用对应功能图标 + 标题 + 副标题 + 登录按钮
- **交互增强**
  - 新增 `.rec-result-card` 推荐结果卡片悬停效果（上浮+阴影+边框高亮）
  - 新增 `.rec-privacy-option` 隐私选项悬停效果（上浮+阴影）
  - 新增 `.setting-item` 设置项悬停效果（背景高亮）
- 9 个文件改动，涉及 8 个页面/组件
- commit: 617915c
- push 失败（网络不可用）

## 迭代 10 (00:46, 5月15日)
- **Bug 修复：构建错误**
  - Recommendations.jsx: `</div>` 闭合标签与 `<PageLayout>` 开标签不匹配，修正为 `</PageLayout>`
  - Recommendations.jsx: 添加缺失的 `PageLayout` import
  - ForumFeed.jsx: 添加缺失的 `PageHero` import（组件使用了 PageHero 但未导入）
  - 修复前 `npx vite build` 报错，修复后构建成功 (322KB JS + 112KB CSS)
- **调研：未使用 PageLayout/PageHero 的页面**
  - 已迁移：About, ForumFeed, Home, Login, Rankings, Recommendations, Register, Settings (8个)
  - 未迁移：AdminPage, ComparePage, DiaperDetail, MessagesPage, NotificationsPage, PostDetail, Profile, TermWiki, UserPage (9个)
  - 下次迭代可统一迁移这些页面
- commit: c074cd9
- push 成功: c074cd9

## 迭代 11 (01:16, 5月15日)
- **DiaperDetail 统一使用 PageLayout**
  - 使用 `PageLayout` + `PageHero` 替代手动 div 布局
  - 移除冗余的品牌/型号重复显示（PageHero 已展示标题+副标题）
  - 统一卡片间距为 PageLayout 的 `gap-5`（20px）
  - 分享按钮移入 PageHero 右侧，与其它页面保持一致
  - 返回按钮集成到 PageHero 中
- **Profile 统一使用 PageLayout**
  - 外层包裹 `PageLayout maxWidth={700}`
  - 未登录状态也使用 PageLayout + hero-card
- **布局统一进度**
  - 已迁移：About, DiaperDetail, ForumFeed, Home, Login, Profile, Rankings, Recommendations, Register, Settings (10个)
  - 未迁移：AdminPage, ComparePage, MessagesPage, NotificationsPage, PostDetail, TermWiki, UserPage (7个)
- commit: eaf6263
- push 成功: eaf6263
